# Example ISE REST API Credentials
# Load them using the `source` command: source ise_environment.sh
# Verify them using `env` for all of your environment variables
# or print them individually using `echo $ISE_HOSTNAME`
export ISE_HOSTNAME=1.2.3.4           # hostname or IP address of ISE PAN
export ISE_REST_USERNAME=admin        # ISE REST API admin or operator username
export ISE_REST_PASSWORD='ISEisC00L'  # ISE REST API admin or operator password
export ISE_CERT_VERIFY=false          # validate the ISE certificate or not
