-- ISE Reports > Diagnostics > AAA Diagnostics
SELECT *
    -- timestamp_timezone,
    -- timestamp,
    -- session_id,
    -- ise_node,
    -- username,
    -- message_severity,
    -- message_code,
    -- message_text,
    -- category,
    -- info
FROM aaa_diagnostics_view
-- FETCH FIRST 10 ROWS ONLY